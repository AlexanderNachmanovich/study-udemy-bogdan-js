// ==============================================================================
// Оператор разделения объекта на свойства
/**
const buttonInfo = {
  text: "Buy",
};

const buttonStyle = {
  color: "yellow",
  width: 200,
  height: 300,
};

const button = {
  ...buttonInfo,
  ...buttonStyle,
};

console.table(button);
*/

// ==============================================================================
// Конкатенация с помощью шаблонных строк
/**
const hello = "Hello";
const world = "World";

const greetings = `${hello} ${world}`;
console.log(greetings);

const myName = "Alexander";
const myCity = "Be'er Sheva";

console.log(`My name is ${myName} and I'm living in ${myCity}`);
*/
// ==============================================================================
